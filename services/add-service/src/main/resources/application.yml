server:
  port: 8081

spring:
  jackson:
    deserialization:
      fail-on-unknown-properties: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

info:
  app:
    name: add-service
    version: 1.0.0
