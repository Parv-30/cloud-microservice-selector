server:
  port: 8084

spring:
  jackson:
    deserialization:
      fail-on-unknown-properties: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

info:
  app:
    name: divide-service
    version: 1.0.0
